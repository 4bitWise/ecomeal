// All app's enums here

/// [ViewModelState]
/// Enum used to manage a viewmodel's state directly from the viewmodel.
enum ViewModelState {
  busy,
  idle,
}

/// [UnitType]
/// Enum used to represent the MeasureUnit type
enum UnitType {
  weight,
  volume,
}
